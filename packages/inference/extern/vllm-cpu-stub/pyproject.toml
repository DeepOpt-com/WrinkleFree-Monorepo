[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "vllm"
version = "0.0.1+cpu.stub"
description = "CPU-only stub for vllm providing PyTorch fallback implementations"
requires-python = ">=3.10"
dependencies = ["torch"]

[tool.setuptools.packages.find]
where = ["src"]
