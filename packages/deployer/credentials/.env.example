# WrinkleFree Deployer Credentials
# Copy to .env and fill in your values

# =============================================================================
# REQUIRED: Cloud Provider API Keys
# =============================================================================

# RunPod API Key (get from https://www.runpod.io/console/user/settings)
export RUNPOD_API_KEY=your_runpod_api_key_here

# =============================================================================
# REQUIRED: Logging & Tracking
# =============================================================================

# Weights & Biases API Key (get from https://wandb.ai/authorize)
export WANDB_API_KEY=your_wandb_api_key_here

# =============================================================================
# REQUIRED: GCS Checkpoints (for checkpoint persistence)
# =============================================================================

# Option 1: Service account JSON file (recommended for cloud deployments)
# Place gcp-service-account.json in this folder
# The smoke_test.yaml will mount it automatically

# Option 2: Base64-encoded JSON (for CI/CD or env-only setups)
# export GOOGLE_APPLICATION_CREDENTIALS_JSON='<base64-encoded-json>'

# =============================================================================
# OPTIONAL: HuggingFace (for private models/datasets)
# =============================================================================

# HuggingFace Token (get from https://huggingface.co/settings/tokens)
# export HF_TOKEN=your_hf_token_here

# =============================================================================
# OPTIONAL: Vast.ai (cheap H100s, marketplace pricing)
# =============================================================================

# Vast.ai API Key (get from https://vast.ai/console/cli/)
# export VASTAI_API_KEY=your_vastai_api_key_here

# =============================================================================
# OPTIONAL: Nebius (alternative to RunPod, cheaper H100s)
# =============================================================================

# Nebius requires OAuth setup, not API key
# Run: ./nebius-setup.sh (downloads from Nebius docs)
# See: docs/research/skypilot-setup.md

# =============================================================================
# OPTIONAL: Modal (legacy, being deprecated)
# =============================================================================

# Modal Token (only if using modal_deployer.py)
# export MODAL_TOKEN_ID=your_modal_token_id
# export MODAL_TOKEN_SECRET=your_modal_token_secret
